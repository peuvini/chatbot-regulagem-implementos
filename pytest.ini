[pytest]
testpaths = tests
addopts = -q --cov=app/backend/app --cov-report=term-missing
markers =
    unit: testes unitários de regras isoladas
    smoke: verificações rápidas de inicialização e rotas essenciais
    regression: cenários que protegem correções de bugs
